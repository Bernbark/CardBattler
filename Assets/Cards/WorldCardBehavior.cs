using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * This class is designed to handle the behavior of a Card when it is instantiated in the game world.
 * In this design, only fighters will be world cards, but this needs to be expanded to handle other cards.
 */
public class WorldCardBehavior : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
